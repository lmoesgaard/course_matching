[project]
name = "course-matching"
version = "0.1.0"
description = "Embedding-based cross-university course matching (SDU vs KU) with interactive visualization"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "sentence-transformers>=6.0.0",
]
